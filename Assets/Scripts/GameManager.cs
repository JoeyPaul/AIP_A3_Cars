using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // lapsAI_1 = 1;
    // lapsAI_2 = 1;
    // lapsAI_3 = 1;
    // lapsPlayer = 1;

    // when the player/ai passes go += 1 to laps
    // when it hits 3 whoever hits 3 first wins

    // UI element pops us that says the player/AI that wins 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
